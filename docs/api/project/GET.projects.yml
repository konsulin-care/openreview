info:
  name: get projects
  type: http
  seq: 9

http:
  method: GET
  url: "{{baseUrl}}/api/v1/project"
  header:
    - name: Content-Type
      value: application/json

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "200"
  scripts:
    - type: tests
      code: |-
        test("projects: returns array", function() {
          expect(Array.isArray(res.body)).to.be.true;
        });
    - type: after-response
      code: |-
        bru.runner.setNextRequest("get project by id");
